package interview.organiser.constants;

/**
 * Enum representing candidate status in interview process
 */
public enum CandidateStatus {
    INVITED,
    INVITATION_ACCEPTED,
    INVITATION_DECLINED,
    SELECT_FOR_NEXT_ROUND,
    SELECTED,
    REJECTED
}

